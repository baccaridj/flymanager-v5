{
  "name": "flymanager-backend",
  "private": true,
  "scripts": {
    "dev": "../.venv/bin/python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8002",
    "test": "PYTHONPATH=. ../.venv/bin/python -m pytest tests"
  }
}
